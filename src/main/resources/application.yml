engine:
  data-source:
    type: CSV
    csv:
      file-path: market_data.csv
#      replay-speed: 1x
      replay-speed: MAX

isotope:
  kite:
    api-key: "xxx"
    access-token: "xxx"
    api-secret: "xxx"
  strategy:
    enabled: true
    risk-limit: 10000
  backtest:
    initial-capital: 1000000.0

logging:
  level:
    root: INFO
    com.isotope: DEBUG
