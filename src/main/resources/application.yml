engine:
  data-source:
    type: CSV
    csv:
      file-path: market_data.csv
#      replay-speed: 1x
      replay-speed: MAX

isotope:
  kite:
    api-key: "xxx"
    access-token: "xxx"
    api-secret: "xxx"
  strategy:
    enabled: true
    risk-limit: 10000
    allocation-per-leg: 2000000.0
#    execution-mode: FUTURES
    execution-mode: SYNTHETIC
  backtest:
    initial-capital: 200000.0

logging:
  level:
    root: INFO
#    com.isotope: DEBUG
